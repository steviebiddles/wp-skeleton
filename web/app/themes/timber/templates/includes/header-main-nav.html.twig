{% for item in menu_items %}
    <li id="nav-link-{{ item.id }}" class="nav-item {{ item.classes | join(' ') }}">
        <a class="nav-link" href="{{ item.get_link }}">{{ item.title }}</a>
    </li>
{% endfor %}